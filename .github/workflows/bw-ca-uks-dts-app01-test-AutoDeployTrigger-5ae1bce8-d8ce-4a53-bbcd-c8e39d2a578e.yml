name: Trigger auto deployment for bw-ca-uks-dts-app01-test

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/bw-ca-uks-dts-app01-test-AutoDeployTrigger-5ae1bce8-d8ce-4a53-bbcd-c8e39d2a578e.yml'

  # Allow manual trigger 
  workflow_dispatch:

permissions:
  packages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          # creds: ${{ secrets.BWCAUKSDTSAPP01TEST_AZURE_CREDENTIALS }}
          creds: |
            {
              "clientId": "2fab8023-7037-4abe-8009-0ef881eb04bd",
              "clientSecret": "Vqi8Q~tEg5mZzgGqizE_CrgrqRRFFEpLRCG9idhZ",
              "subscriptionId": "1abc1d2b-b0fd-4cdd-a46a-9407e58cea59",
              "tenantId": "2d8fc7e0-16dd-4e68-80d9-255d262f21d3"
            }

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: ghcr.io
          # registryUsername: ${{ secrets.BWCAUKSDTSAPP01TEST_REGISTRY_USERNAME }}
          # registryPassword: ${{ secrets.BWCAUKSDTSAPP01TEST_REGISTRY_PASSWORD }}
          registryUsername: ${{ github.actor }}
          registryPassword: ${{ secrets.GITHUB_TOKEN }}
          containerAppName: bw-ca-uks-dts-app01-test
          resourceGroup: bw-rg-uks-test-appservices-02
          imageToBuild: ghcr.io/tonytregcdw/docker-flask-server2:${{ github.sha }}
          
            

     

